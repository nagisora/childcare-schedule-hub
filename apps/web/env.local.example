# Supabase 接続情報
# このファイルを `.env.local` にコピーし、実際の値を設定してください。
#
# 設定手順:
# 1. Supabase プロジェクトのダッシュボード (https://app.supabase.com) にアクセス
# 2. Settings > API から以下を取得:
#    - Project URL → NEXT_PUBLIC_SUPABASE_URL
#    - anon public key → NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - service_role key → SUPABASE_SERVICE_ROLE_KEY
# 3. このファイルを `.env.local` にコピーし、値を設定
# 4. `.env.local` は Git にコミットしない（.gitignore に含まれている）

# クライアントから参照可能（NEXT_PUBLIC_ プレフィックスを付与）
NEXT_PUBLIC_SUPABASE_URL="https://<project>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# サーバー専用（クライアントに公開しない）
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJI..."

# フェーズ9: Google Custom Search API（Programmable Search Engine）
# 注意: APIキーをクライアントに露出させないこと。ログ出力も禁止。
GOOGLE_CSE_API_KEY=""
GOOGLE_CSE_CX=""

# オプション: Supabase CLI を使う場合
SUPABASE_DB_PASSWORD=""

